services:
  - type: web
    name: legal-doc-chatbot
    env: python
    region: oregon          # You can choose the closest region to you
    plan: free              # Free tier, suitable for small projects
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn app:app --host 0.0.0.0 --port $PORT"
